import { Link } from "react-router-dom";

/**
 * Page not found (404) component
 */
export const NotFound = () => (
  <div className="not-found">
    <img src="/images/loading.gif" style={{margin: "0 auto"}} width="100" height="100" />
  </div>
);
